type Movie {
  id: Int!
  title: String!
  rating: Float!
  summary: String!
  lanaguage: String!
  medium_cover_image: String!
}

type Query {
  allMovies: [Movie]!
  movies(limit: Int, rating: Float): [Movie]!
}
